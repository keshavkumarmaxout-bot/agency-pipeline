# Contract template

TODO for the agency: replace with your real, lawyer-reviewed agreement. The agent
only fills the variable fields below — it must never draft or alter legal clauses.

## Variable fields the agent may populate

- `client_legal_name`, `client_address`
- `services_summary` (from the accepted proposal's scope section)
- `total_fee`, `payment_schedule` (from the accepted proposal's investment section)
- `start_date`, `end_date`
- `signatory_name`, `signatory_email`

## Fixed clauses — never modified by an agent

Liability, IP assignment, confidentiality, termination, governing law, dispute
resolution. These are your lawyer's text. If a deal needs a change to any of
them, that is a human task and the agent must escalate.
